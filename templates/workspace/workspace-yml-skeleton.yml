# workspace.yml — single source of truth for this workspace.

workspace:
  name: PLACEHOLDER_WS_NAME
  xcworkspace: true
  code_workspace: true
  tasks:
    enabled: true
    mode: sibling          # sibling | path | symlink
    path: ./Tasks          # used by sibling / path modes
    symlink_target: null   # required by symlink mode (e.g. ../../Tasks)
  docs:
    enabled: true
    mode: sibling          # sibling | path | symlink
    path: ./Docs           # used by sibling / path modes
    symlink_target: null   # required by symlink mode (e.g. ../../Docs)

toolkit:
  lang: en                 # en | ru — the toolkit config's language, and the dialog's
  mode: manual             # manual | auto
  progress: normal         # quiet | normal | live

remotes:
  - origin

defaults:
  default_branch: main
  push_remotes: all
  release_strategy: patch
  platforms:              # floor of generated packages; keys: ios | macos
    ios: "17.0"
    macos: "14.0"
  tests: swift-testing    # swift-testing | xctest | quick-nimble — packages, and the apps

bootstrap:
  use_gh: false
  push_after_init: false
  generate_local_skills: false
  commit_after_init: true
  initial_commit_message: Initial commit
  git_author: null

packages:
  # - name: Example
  #   archetype: library       # api-contract | engine | library | feature
  #   version: "0.1.0"         # M.m.p semver
  #   git: { origin: git@github.com:user/Example.git }
  #   deps: []
  #   external_deps:           # version kinds: from | exact | branch | revision; semver is M.m.p
  #     - url: https://github.com/user/Dep1.git
  #       version: { from: "1.0.0" }
  #     - https://github.com/user/Dep2.git
