workspace:
  name: QuickWS
remotes: [origin]
defaults:
  tests: quick-nimble
packages:
  - name: OnePkg
    archetype: api-contract
    git: { origin: git@github.com:user/OnePkg.git }
    version: 0.1.0
